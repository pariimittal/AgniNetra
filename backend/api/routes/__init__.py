"""
API route modules, one per resource area.

Phase 2 scope: only health.py exists. Future phases will add
events.py, facilities.py, etc. — each should define its own
`APIRouter` and be included from backend/main.py, keeping this
package as the single place routers are collected.
"""
