"""
Pydantic response/request schemas.

Phase 2 scope: only the health-check schema exists so far. Future phases
will add ThermalEvent / Facility / FingerprintData / FacilityHistory
schemas here, mirroring src/types/index.ts on the frontend exactly (see
Phase 1 audit, section C, for the frozen field-by-field contract).
"""
