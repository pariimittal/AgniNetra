"""
Service / adapter layer (business logic that sits between API routes and
data sources), e.g. turning a raw anomaly + ML outputs into the frontend's
ThermalEvent shape.

Empty in Phase 2 (backend foundation only) — intentionally left as a
placeholder package so routes have somewhere to import services from once
that work begins.
"""
